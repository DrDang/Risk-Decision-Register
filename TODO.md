# TODO

- Automatically make slides.
- Add individual risk or decision copy capability for emails.
- For CSV export of decisions, if there is no linked risk, have that cell say `N/A` instead of blank.
- Make default mitigation plan text red if not set by the user. Keep it not red when the text is `Define the mitigation plan for this risk.`
- Remove draft recovery because it does not work reliably.
- Update the decision tool to export decisions in ADR format.
- Add ADR markdown import capability.
- Add an auto-save feature.
- Add chart edit history range controls.
